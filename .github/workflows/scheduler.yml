name: scheduler

on:
  schedule:
    - cron: '0/5 * * * *'
  workflow_dispatch:
    
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: first_step
        env:
          TEST: ${{ secrets.test }}
        run: |
          curl --url "https://aa20-68-108-187-54.ngrok.io" --data 'message="$TEST"'
